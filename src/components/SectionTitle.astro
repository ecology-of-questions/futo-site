---
/**
 * SectionTitle.astro
 * ------------------------------------------------------------
 * 役割: 各セクション(Research Statement / 今、取り組んでいること /
 * 研究断面 / 研究便り 等)共通の見出しコンポーネント。
 * Design Spec 11 Components: Section Title
 *
 * 各セクションで見出しの見せ方(サイズ・余白)を統一するための
 * 共通部品として切り出す。
 * ------------------------------------------------------------
 */

interface Props {
  as?: "h1" | "h2" | "h3";
}

const { as: Tag = "h2" } = Astro.props;
---

<!-- TODO: 実装レベルの仕様到着後に実装 -->
