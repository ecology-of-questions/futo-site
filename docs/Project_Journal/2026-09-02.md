# 2026-09-02

- **Fieldnote Reading 最小プロトタイプを実装**(`/fieldnote/`、未
  リンク): 本のタイトル入力→読書セッション開始→カメラプレビュー
  維持→1タップで静止画保存(確認画面なしですぐカメラへ戻る)→
  セッション終了→撮影ページの一覧表示、という最小フローのみを
  実装。OCR・AI・ログイン・課金・ISBN検索・タグ・引用範囲選択・
  Apple Watch連携・集中モード連携は実装していない。
- 画像はセッションごとに端末内(IndexedDB)へ保存し、保存処理は
  `FieldnoteStore`インターフェースでUIから分離(将来Supabase等へ
  差し替え可能な構成)。iPhone Safari・ホーム画面追加PWAを優先し、
  Web App Manifestと関連メタタグを追加した。
- `npm run build`成功を確認。詳細な設計判断はDecision Log 0058参照。
