# 2026-08-08

- `npm run dev` で実機確認したところ、Heroに3つの表示崩れが見つかった。
  見出し「そこに問いがある。」がデスクトップ幅で意図しない位置
  (「あ」の直後)で折り返される、VisualFragmentの画像が
  object-fit:coverで左右トリミングされ不自然に見える、containに
  変更しても正方形に近い枠と横長画像の比率が合わず上下に余白が残る、
  という3点。
- `headingLines`を3行に分割し`word-break: keep-all`を追加、
  VisualFragmentを`object-fit: contain`に変更、Heroのビジュアル枠に
  `aspect-ratio: 3 / 2`を設定し、コピー列の幅を`42%`から
  `minmax(280px, 22rem)`に変更してビジュアルへより多くの幅を渡す形に
  調整した(Decision Log 0018)。
- コピー列の高さとビジュアル枠の高さを完全に一致させるにはフルブリード
  レイアウトへの変更が必要になることが分かったが、Design Spec・
  モックアップの範囲を超える視覚的な方向転換のため、今回は見送り、
  今後ChatGPT側のレビューが必要な項目として記録した。
