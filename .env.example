# futo-site 環境変数サンプル
# 実際の値は .env / .env.production に設定する(いずれもgitignore済み)。

# Amazonアソシエイトのトラッキングタグ。未設定のままでよい。
# 設定した場合のみ、購入リンク(purchaseProvider: "amazon" かつ
# affiliate: true の本)にタグが付与される(src/config/site.ts参照)。
PUBLIC_AMAZON_ASSOCIATE_TAG=
